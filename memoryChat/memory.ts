export const saveMessageToMemory = (message: string) => {
  /**
   * NOTE: 実際にはVectorStoreに保存する処理を実装する必要があります。
   * おすすめはMetal(https://getmetal.io/)
   */
  return true
}

export const searchMessagesFromMemory = (query: string): string[] => {
  /**
   * NOTE: 実際にはVectorStoreに保存する処理を実装する必要があります。
   * おすすめはMetal(https://getmetal.io/)
   */
  return ["私はお寿司が好きです"]
}
